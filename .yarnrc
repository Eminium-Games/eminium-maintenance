# This will ensure Yarn uses the correct Node.js version
nodeLinker: node-modules

# This will ensure the same package versions are installed across all environments
enableImmutableInstalls true

# This will ensure the same package versions are installed across all environments
enableGlobalCache true

# This will ensure the same package versions are installed across all environments
enableScripts true

# This will ensure the same package versions are installed across all environments
nodeLinker: node-modules

# This will ensure the same package versions are installed across all environments
enableTelemetry false
